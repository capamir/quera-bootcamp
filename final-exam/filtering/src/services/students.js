import { config } from "../constants/config";

export const getStudents = async (queryString) => {
    // expect to send a requet to /students with query string if needed
    // http://127.0.0.1:5000/students

    // return array of objects
};